machine:
  xcode:
    version: "6.3.1"
  environment:
    XCODE_SCHEME: Archive

dependencies:
  pre:
    - brew install npm

test:
  override:
    - npm install && npm test

deployment:
  production:
    branch: release
    commands:
      - echo "finished"
  staging:
    branch: master
    commands:
      - echo "finished"
