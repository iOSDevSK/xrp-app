.margin50.white.center
    h1.light query account 

    if error
        p
         | Error: Could not properly parse QR code

    if cameraNotEnabled
        p.camera-not-enabled
         | camera not enabled
         | cannot query accounts with QR

    p
     | tap the screen 
    p
     | scan a QR code of a ripple address 

    if account
        #qr-query.qr-account-detail.qr
        p
            span
             | Balance
            span
             = "  " + account.balance + "XRP"
    else
        #qr-query.qr-account-default.qr
